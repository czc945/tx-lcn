 spring:
  cloud:
    config:
      name: cloud-config
      uri: http://config.zhicong.site
      profile: ${config.profile:dev}
      label: ${config.label:master}
      username: ${CONFIG_SERVER_USER:root}
      password: ${CONFIG_SERVER_PASSWORD:admin}